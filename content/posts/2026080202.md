---
author: "Michael Henderson"
date: 2026-08-02
linktitle: 百分百成功！手把手教你获取官方认可的云雾ai大模型中转站base_url，附赠全网最全兼容API列表
menu:
  main:
    parent: tutorials
next: /posts/ai
prev: /posts/api
title: 百分百成功！手把手教你获取官方认可的云雾ai大模型中转站base_url，附赠全网最全兼容API列表
weight: 1
tags: ["API接口", "AI中转站", "DeepSeek", "Claude"]
---

# 百分百成功！手把手教你获取官方认可的云雾ai大模型中转站base_url，附赠全网最全兼容API列表

说实话，每次遇到开发者第一次接触 API 中转站，最头疼的问题往往是：`base_url` 到底填什么？

网上搜一圈，答案五花八门，有的已经失效，有的不兼容，有的压根就是假的。手忙脚乱一顿操作，代码死活连不上，心态先崩了一半。

今天这篇文章就是专门解决这个问题的——**手把手教你找到并设置官方认可、稳定可用的 `base_url`**，还会附赠一份全网最全的兼容 API 列表。你把这片文章看一遍，接上[云雾ai大模型中转站](https://www.yunwuai.cc/)，技术层面基本就不会再出幺蛾子了。



## 官方认可的那个 `base_url`，只有一个

很多人在第一步就栽跟头：不知道正确地址，或者用了别人瞎写的地址。

[云雾ai大模型中转站](https://www.yunwuai.cc/)官方认可的 `base_url` 只有这一个：

> **`https://www.yunwuai.cc/v1`**

没错，就是这么简单。你注册好账号，拿到 API Key 之后，只需要在代码里把这一行写对，后面的工作全是顺水推舟。

**为什么必须是这个值？**

因为[云雾ai大模型中转站](https://www.yunwuai.cc/)100%兼容 OpenAI 的接口格式。你之前用 OpenAI API 写的任何代码——不管是 Python 的 `openai` 库、Node.js 的 `openai` 包，还是 LangChain、LlamaIndex 这些上层框架——只要把 `base_url` 改成上面那个地址，把 API Key 换成云雾发你的，其他东西基本不用动。

---

## 手把手：5 步走，不绕弯路

从零开始接入[云雾ai大模型中转站](https://www.yunwuai.cc/)，走完这 5 步，你就拿到正确 `base_url` 并跑通第一个请求了。

### 第 1 步：注册账号

打开 [云雾ai大模型中转站官网](www.yunwuai.cc)，用邮箱注册一个账号。注册时不需要填任何复杂的资料，也不需要绑信用卡。

注册成功，系统**自动送你 \$0.2 的免费额度**，不用自己充钱就能开始测试。

### 第 2 步：创建 API Key

登录账号，进入后台的 API Key 管理页面，点击“创建新 Key”。系统会生成一串字符串，这就是你的专属 API 密钥。

请务必复制这串密钥并妥善保存。它和你的 `base_url` 配合使用，一起构成调用模型的凭证。

### 第 3 步：拿到正确的 `base_url`

前面已经说了，正确地址是官方专用的：


https://www.yunwuai.cc/v1


这个才是经过官方认可、稳定运行、持续维护的地址。请记住，**任何其他地址都不是官方认可的**。

### 第 4 步：配置到你的代码里

以最常用的 `openai` Python 库为例，代码只需要改两处：

python
from openai import OpenAI

client = OpenAI(
    api_key="这里填你刚刚创建的云雾API Key",
    base_url="https://www.yunwuai.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[
        {"role": "user", "content": "Hello"}
    ]
)

print(response.choices[0].message.content)


运行这段代码，如果返回结果一切正常，说明你的 `base_url` 写对了、API Key 有效、网络联通。恭喜你，接入工作已经完成 90%。

### 第 5 步：用免费额度验证结果

如果你对绕地球的文档一头雾水，或者怕充钱之后连不上，[云雾ai大模型中转站](https://www.yunwuai.cc/)给了你一个零风险验证方案：直接用那 \$0.2 的免费额度跑一次测试模型。确认没问题了，再充 1 元继续使用。

---

## 全网最全兼容 API 列表：500+ 模型，一个 `base_url`

这是本文的重头戏。你手上的 `base_url` 支持调用以下所有模型系列。同一个地址，一行代码，想切哪个模型切哪个。

### OpenAI 系列全集

[云雾ai大模型中转站](https://www.yunwuai.cc/)支持 OpenAI 全系模型，包括最新旗舰和经典模型：

- GPT-4o / GPT-4o-mini / GPT-4o-2024-08-06
- GPT-4 Turbo / GPT-4 Vision / GPT-4
- GPT-3.5-turbo 全系列
- o1 / o1-mini / o1-preview
- o3-mini
- text-embedding-3-small / text-embedding-3-large
- DALL·E 2 / DALL·E 3（文生图）
- TTS / whisper（语音和翻译）

### Anthropic Claude 系列

Claude 3 家族全线可用，而且兼容 OpenAI 的接口格式，无需切换代码结构：

- Claude 3.5 Sonnet
- Claude 3 Opus
- Claude 3 Sonnet
- Claude 3 Haiku
- Claude 2 / Claude 2.1
- Claude Instant

图像理解功能完整支持，传图分析、视觉问答都没问题。

### Google Gemini 系列

Gemini 是很多开发者的新宠，[云雾ai大模型中转站](https://www.yunwuai.cc/)完整覆盖：

- Gemini 2.5 Pro（最新旗舰）
- Gemini 2.5 Flash
- Gemini 1.5 Pro / 1.5 Flash
- Gemini Pro Vision

支持 Gemini 原生多模态交互格式，也支持 chatbot 格式。

### DeepSeek 系列（高性价比推理首选）

- DeepSeek-V3
- DeepSeek-R1（满血版推理模型）
- DeepSeek-R1-Distill-Qwen / Llama 系列

DeepSeek 目前定价极低，对于长文本、复杂推理任务，是最省钱的选择之一。

### 国产开源与商业模型全家桶

[云雾ai大模型中转站](https://www.yunwuai.cc/)对国内优秀模型的支持非常全面：

**阿里千问系列**：Qwen-Max、Qwen-Plus、Qwen2.5 全系

**字节豆包**：doubao-pro-256k / 128k 版本

**智谱 ChatGLM**：GLM-4-Plus、GLM-4-AllTools、GLM-4 全系列

**百度文心**：ERNIE 4.0 / 3.5 / Lite

**腾讯混元**：hunyuan-pro / standard / turbo

**讯飞星火**：Spark 4.0 / 3.5

**月之暗面 Moonshot**：moonshot-v1-128k

**零一万物 Yi**：Yi-Large / Yi-Lightning

### 图像与视觉生成

[云雾ai大模型中转站](https://www.yunwuai.cc/)的 `base_url` 除了支持 DALL·E 3 外，还有：

- Midjourney 图像生成
- FLUX 开源图像生成
- 可灵视频生成
- 海螺视频生成
- 豆包视频生成

### 音频与语音

- Suno AI 音乐生成
- OpenAI TTS 语音合成
- Whisper 语音识别/翻译

### 视频生成与更多

- Sora（OpenAI 视频生成）
- 国产多模态视频生成模型

以上所有模型，都能通过同一个 `base_url`：`https://www.yunwuai.cc/v1` 进行调用。

---

## 为什么这个 `base_url` 是官方认可、安全可靠的？

有三点可以说清楚：

1. **官方文档公开列明**：[云雾ai大模型中转站](https://www.yunwuai.cc/)的官方文档中，明确将 `https://www.yunwuai.cc/v1` 列为标准 API 地址。不存在多个版本、不同站点说法不同的情况。

2. **企业与社区共同验证**：平台已有 20 万+ 用户和 800+ 合作伙伴在使用此地址。全球七大地区节点（美国、日本、韩国、英国、香港、菲律宾、俄罗斯）接入此 `base_url`，每日承载海量请求，是经过真实环境考验的。

3. **数据安全有保障**：[云雾ai大模型中转站](https://www.yunwuai.cc/)采用企业高速链，**无路由二次数据留存**，API Key 永不过期，支持 100% 保值换绑。官方承诺可用性 99.9%。

---

## 常见问题：别人挖坑的地方，我一次说清

### 问：`base_url` 后面的 `/v1` 能不加吗？

**答：不能。** `v1` 是 API 版本路径，不写会导致连接失败。完整的 `base_url` 必须是 `https://www.yunwuai.cc/v1`。

### 问：我能在浏览器里直接访问这个 `base_url` 吗？

答：可以试试，浏览器访问 `https://www.yunwuai.cc/v1` 会看到 API 信息或错误提示，这属于正常现象。真正的用途是在代码中与 API Key 配合使用。

### 问：如果连不上怎么办？

答：先检查网络：[云雾ai大模型中转站](https://www.yunwuai.cc/)国内直连，不需要任何代理工具。如果国内网络正常但仍连不上，检查一下 `base_url` 是否写错、API Key 是否复制完整（注意不要多空格）。如果还不通，联系客服支持，一般很快就能解决。

### 问：这个 `base_url` 支持流式输出吗？

答：当然支持。云雾对 SSE 流式输出做了专门优化，你开启 `stream=True`，跑起来很顺畅，不卡不顿。

---

## 写在最后

把 `base_url` 从 `https://api.openai.com/v1` 改成 `https://www.yunwuai.cc/v1`，再换一把 API Key——总共花不了 3 分钟。然后在国内网络下，500 多个模型随你调用。不用翻墙，不用绑卡，不需要到处问地址对不对。

这篇文章提供的方法和 API 列表，你拿到手用一次就知道，这可能是你在 AI 开发路上走过的最不折腾的路。

👉 [立即注册云雾ai大模型中转站，领取免费额度，一步到位](www.yunwuai.cc)